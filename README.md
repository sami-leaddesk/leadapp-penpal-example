# LeadApp example application

Example web application on using leadapp integration library

## Usage

Quick start quide

```sh
npm i
npm run start
```

Should open <http://localhost:8080> automatically

Page does not do anything itself, but you need to embed this application inside LeadDesk UI to be able to send the actual requests.
